/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com', 'img.freepik.com', 'lh3.googleusercontent.com', 'res.cloudinary.com'],
    },
};

export default nextConfig;
